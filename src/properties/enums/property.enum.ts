export enum PropertyStatus {
  AVAILABLE = 'available',
  SOLD = 'sold',
  RENTED = 'rented',
}

export enum PropertyOrderBy {
  PRICE = 'price',
  CREATED_AT = 'createdAt',
}

export enum OrderDirection {
  ASC = 'ASC',
  DESC = 'DESC',
}
